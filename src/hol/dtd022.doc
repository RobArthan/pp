=TEX
% TQtemplate.tex
\documentstyle[hol,11pt,TQ]{article}
\ftlinepenalty=9999
\def\Hide#1{}
\def\Bool{``$\it{:}bool\,$''}
\makeindex
\TPPproject{FST PROJECT}  %% Mandatory field
%\TPPvolume{}
%\TPPpart{}
\TPPtitle{Detailed Design for Parser Interface}  %% Mandatory field
\TPPref{DS/FMU/IED/DTD022}  %% Mandatory field
\def\SCCSversion{$Revision$
}
\TPPissue{\SCCSversion}  %% Mandatory field
\TPPdate{\FormatDate{$Date$
}}  %% Mandatory field (with sensible default)
\TPPstatus{Draft}			%% Mandatory field
\TPPtype{Specification}
\TPPkeywords{HOL}
\TPPauthor{D.J.~King & WIN01}  %% Mandatory field
%\TPPauthors{Name 1&location 1\\Name 2&location 2\\Name 3&location 3}
\TPPauthorisation{R.D.~Arthan & FST Team Leader}
\TPPabstract{This document gives the detailed design
of the parser interface.
}
%\TPPabstractB{}
%\TPPabstractC{}
%\TPPabstractD{}
%\TPPabstractE{}
%\TPPabstractF{}
\TPPdistribution{\parbox[t]{4.0in}{%
      Library \\ R.D. Arthan\\ D.J. King}}

%\TPPclass{CLASSIFICATION}
%\def\TPPheadlhs{}
%\def\TPPheadcentre{}
%def\TPPheadrhs{}
%\def\TPPfootlhs{}
%\def\TPPfootcentre{}
%\def\TPPfootrhs{}

\begin{document}
\TPPsetsizes
\makeTPPfrontpage

\vfill
\begin{centering}

\bf Copyright \copyright\ : International Computers Ltd \number\year

\end{centering}

\pagebreak
\section{DOCUMENT CONTROL}
\subsection{Contents list}
\tableofcontents
\subsection{Document cross references}

\bibliographystyle{fmu}
\bibliography{fmu}

\subsection{Changes history}  % to get section number `0.3'

\begin{description}
\item[Issue 1.1]
Initial issue containing Standard ML code only.

\item[Issue 1.2]
Document contains description of purpose, design etc.

\item[Issue 1.3 30 August 1991]
Corrected missing "\_".

\end{description}
\subsection{Changes forecast}
\pagebreak
\section{GENERAL}

\subsection{Scope}

This document provides an interface to the parser and
type checker and comprises a part of ICL HOL.

\subsection{Introduction}

This document contains the signature $ParserInterface$
which provides access to a function $HOL\_term\_recogniser$.
This is called from the reader/writer \cite{DS/FMU/IED/DTD005}.
\subsubsection{Background and Purpose}

The parser interface takes a term, expressed
in HOL concrete syntax, from the reader/writer
and yields a term of type $TERM$ which is correctly
type\_checked. The parser interface itself does not
report failures or errors. Reporting is the responsibility of
the parser and the type inference mechanism.
It is not intended that the parser interface be called
directly. The intention is that when
the reader/writer detects
input beginning with ``»'' and ending with ``¼'', it
calls the function $HOL\_term\_recogniser$ which analyses,
parses
and type-checks the input stream to produce a HOL term.

\subsubsection{Interface}

This document defines a signature $ParserInterface$.

\subsubsection{Deficiencies}

None Known.

\subsubsection{Dependencies}

\section{THE SIGNATURE}

=DOC
signature âParserInterfaceá = sig
	local
		open Lex Parser
	in
		val HOL_term_recogniser : 'a * 'b * INPUT list * 'c -> TERM
		val HOL_type_recogniser : 'a * 'b * INPUT list * 'c -> TYPE

	end

end; (* of signature ParserInterface *)
=DESCRIBE
This is the signature for the structure ParserInterface.
=ENDDOC

\section{TEST POLICY}
The tests should follow the standards and guidelines
laid down in \cite{DS/FMU/IED/PLN008}.

\twocolumn[\section{INDEX OF DEFINED TERMS}]
\printindex
\end{document}

